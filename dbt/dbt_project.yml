name: football
version: "1.0.0"
config-version: 2

profile: football

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  football:
    staging:
      +materialized: view
    marts:
      +materialized: incremental
