apiVersion: 1

providers:
  - name: football-pipeline
    orgId: 1
    folder: Football Pipeline
    folderUid: football-pipeline
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
