# football-pipeline-v6 — Python dependencies
# Install: pip install -r requirements.txt

# --- Core ---
pandas==2.2.2
pyarrow==15.0.2
psycopg2-binary==2.9.9

# --- Spark ---
pyspark==3.5.1

# --- Kafka ---
confluent-kafka==2.4.0

# --- Airflow (local dev / CI — not used inside Docker which installs via _PIP_ADDITIONAL_REQUIREMENTS) ---
apache-airflow==2.9.1
apache-airflow-providers-postgres==5.11.0

# --- dbt ---
dbt-core==1.8.3
dbt-postgres==1.8.3

# --- Testing ---
pytest==8.2.2
pytest-cov==5.0.0
